[build]
  command = "npm run build"
  publish = ".next"

[[plugins]]
  package = "@netlify/plugin-nextjs"

[build.environment]
  NODE_VERSION = "20"

# Next.js는 플러그인이 자동으로 리다이렉트를 처리합니다
# API 라우트는 자동으로 Netlify Functions로 변환됩니다

